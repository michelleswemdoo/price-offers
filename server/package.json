{
  "name": "server",
  "version": "1.0.0",
  "description": "price offers server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm install && cd ../frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "Michelle Swem",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
